---
label: Default
hide_body: true
is_partial: false
fields:
- type: select
  name: page_type
  config:
    source:
      type: simple
    options:
    - split
    - default
    required: true
  label: Page Type
  default: split
- type: blocks
  label: Blocks
  name: blocks
  template_types:
  - heading
  - text-block
