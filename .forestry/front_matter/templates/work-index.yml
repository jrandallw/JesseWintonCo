---
label: Work Index
hide_body: false
fields:
- name: projects
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: work
  label: Projects
pages:
- work.markdown
